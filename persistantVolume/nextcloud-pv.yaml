apiVersion: v1
kind: PersistentVolume
metadata:
  name: mnt-nextcloud-pv
  labels:
    app: nextcloud
spec:
  storageClassName: pv1
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  hostPath:
    path: "/mnt/public/nextcloud"